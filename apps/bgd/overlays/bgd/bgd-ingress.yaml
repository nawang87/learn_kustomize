apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lama
spec:
  rules:
    - host: lama.devnation
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lama
                port:
                  number: 8080
    - host: lamadai.devnation
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lamadai
                port:
                  number: 8080
